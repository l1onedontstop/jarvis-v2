# 核心依赖
# torch 2.11 的构建/运行元数据要求 setuptools<82
setuptools<82
numpy>=1.21.0,<2
librosa>=0.9.0
sounddevice>=0.4.0
soundfile>=0.12.0
sherpa-onnx>=1.9.0
# 系统级回声消除（包含其他应用的系统输出参考；按平台安装对应 wheel）
aec-audio-processing-fork==1.0.0.dev1; sys_platform == "darwin"
aec-audio-processing-fork==1.0.1.dev1; sys_platform == "win32"

# TTS
# 注意: 包名是 piper-tts，安装后 import 时用 from piper import PiperVoice
# 切勿 pip install piper（那是另一个无关包 pypiper）
emoji>=2.0.0
piper-tts>=1.0.0
onnxruntime>=1.20.0
# JARVIS-V2 MeloTTS ONNX 中英混合文本前处理与 multilingual BERT 特征
transformers>=4.40.0,<5
torch==2.2.2; sys_platform == "darwin" and platform_machine == "x86_64"
torch>=2.4; sys_platform != "darwin" or platform_machine != "x86_64"
cn2an>=0.5.22
pypinyin>=0.50.0
jieba>=0.42.1
num2words>=0.5.13
# 金属感语音后处理用：自带跨平台静态 ffmpeg 二进制，免系统安装/PATH
imageio-ffmpeg>=0.4.9

# OpenClaw 桥接
requests>=2.28.0
openai>=1.0.0
websocket-client>=1.6.0   # WebSocket 桥 (import websocket)
cryptography>=41.0.0      # WebSocket 桥设备签名 (Ed25519)
